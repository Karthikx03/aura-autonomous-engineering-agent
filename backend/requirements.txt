fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
sqlalchemy==2.0.35
aiosqlite==0.20.0
# Postgres driver (psycopg2-binary) intentionally lives in
# requirements-postgres.txt, not here: the default config (app/config.py)
# uses sqlite+aiosqlite, no app code imports psycopg2, and it's only
# actually connected to via docker-compose.yml's Postgres service. Keeping
# it out of the base install lets `pip install -r requirements.txt` succeed
# on a bare local dev machine that has no PostgreSQL headers installed --
# psycopg2-binary ships prebuilt wheels for common platforms, but not for
# every OS/Python-version combination (e.g. no cp313 wheel on macOS arm64
# yet), which forces a from-source build that fails without `pg_config`.
redis==5.0.8
websockets==13.1
httpx==0.27.2
python-dotenv==1.0.1
GitPython==3.1.43
docker==7.1.0
openai==1.51.0
anthropic==0.34.2
google-generativeai==0.8.2
prometheus-client==0.21.0
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
